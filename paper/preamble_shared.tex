% =====================================================================
% preamble_shared.tex -- common preamble for BOTH build targets (D-054
% pattern). \input by main_tmlr.tex and main_arxiv.tex AFTER \documentclass,
% the \newif\ifanon flag, and the target-specific \usepackage{tmlr} line.
%
% Do NOT load here: natbib, fontenc, lmodern, fancyhdr, eso-pic -- tmlr.sty
% already \RequirePackage's them and fixes the citation style to author-year.
% Do NOT load geometry: tmlr.sty owns the page dimensions. Reloading either
% is a style deviation and a desk-reject ground.
% =====================================================================

\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{booktabs}   % \toprule \midrule \bottomrule (Tables 1-4, App. D)
\usepackage{graphicx}   % figures F1-F5 (vector PDF)
\usepackage{longtable}  % Appendix D, 108 rows

\usepackage{hyperref}
\usepackage{url}
\hypersetup{colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue}

% The artifact reference form is the ONLY content difference between targets
% (besides the author matter, which tmlr.sty itself switches). Named
% repository in the preprint build; in the anonymous build the artifact
% travels WITH the submission as supplementary material (operator ruling
% 2026-07-24), which keeps the release inside the double-blind envelope
% instead of behind a third-party proxy that would require a public repo.
\ifanon
  \newcommand{\ArtifactRepoURL}{Supplementary material accompanying this submission}
\else
  \newcommand{\ArtifactRepoURL}{\url{https://github.com/Sup3rn0vaN0w/mainlabs-reservation-discipline}}
\fi

% =====================================================================
\title{An Evaluation of Reservation-Based Serving for Long-Horizon Agentic
Workloads}

% Hidden automatically by tmlr.sty unless [preprint] or [accepted] is set.
\author{\name Sumit P. Ahuja \email sumit@mainlabs.ai \\
        \addr Main Labs}

% Camera-ready only (consumed by tmlr.sty under [accepted]); placeholders per
% the official template until the forum URL and publication date exist.
\def\month{MM}
\def\year{YYYY}
\def\openreview{\url{https://openreview.net/forum?id=XXXX}}
